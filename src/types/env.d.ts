/// <reference types="vite/client" />

/** Подставляется на этапе сборки из vite.config.ts (define). */
declare const __SOCKET_TARGET__: string;

/** База для загруженных логотипов; в dev это префикс прокси. */
declare const __SPORTSBOOK_URL__: string;
